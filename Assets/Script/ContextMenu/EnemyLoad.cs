using System;
using UnityEngine;

public static class EnemyLoad
{
    [ContextMenu("load")]
    public static void Load()
    {
        // string[] names = Enum.GetNames(typeof(EnemyType));
        // attackData = new EnemyDataPreload.EnemyData[names.Length];
        // hurtData = new EnemyDataPreload.EnemyData[names.Length];
        // for (int i = 0; i < names.Length; i++)
        // {
        //     attackData[i] = new EnemyDataPreload.EnemyData();
        //     hurtData[i] = new EnemyDataPreload.EnemyData();
        //     attackData[i].type = names[i].ToEnum<EnemyType>();
        //     hurtData[i].type = names[i].ToEnum<EnemyType>();
        // }
    }
}